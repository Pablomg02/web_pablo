---
layout: base.njk
---
<article class="content essay">
  <header class="essay__header">
    <h1>{{ title }}</h1>
    {% if date %}
      <time class="essay__date" datetime="{{ date | dateToFormat }}">{{ date | dateToFormat }}</time>
    {% endif %}
  </header>
  {{ content | safe }}
</article>
